<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newWork">


	<insert id="addNewWork" parameterType="newWork">
		INSERT INTO newWork
		(title, company, head, phone_number, number, email, content)
		VALUES
		(#{title}, #{company}, #{head}, #{phone_number}, #{number}, #{email}, #{content})
	</insert>
	

</mapper>